function setup() {
  createCanvas(200, 200);
  background(255);
  smooth();
  fill(192);
  noStroke();
  rect(40,40,40,40);
  
  applyMatrix();
  rotate(radians(45));
  fill(0);
  rect(40,40,40,40);
  resetMatrix();
}

function draw() {
  
}
