function setup(){
	createCanvas(200,200);
  noStroke();
  background(255);
  fill(0,102,153,204);
  smooth();
  noLoop();
}

function draw(){
	circles(40,80);
  circles(90,70);
}

function circles( x, y){
	ellipse(x,y,50,50);
  ellipse(x+20,y+20,60,60);
}
